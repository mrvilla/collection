export interface DomOptions {
    DOM: any;
}
export interface DomPlugin {
    options: DomOptions;
    pluginName: string;
    initialize: any;
}
declare const VeamsDOM: {
    options: {
        DOM: any;
    };
    pluginName: string;
    initialize: (Veams: any, { DOM }: DomOptions) => void;
};
export default VeamsDOM;
